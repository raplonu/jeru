# {{ name }}

This is the project briefing for **{{ name }}** — the committed launch point for
Claude Code. The repos, knowledge sets, and resources listed below are available
as additional directories.
{% if primary_repo %}

Primary repo (cd here for sustained code work): `{{ primary_repo }}`
{% endif %}

## Repos
{% for repo in repos %}
- `{{ repo }}`
{% else %}
_None linked._
{% endfor %}

## Knowledge sets
{% for ks in knowledge_sets %}
- {{ ks }}
{% else %}
_None linked._
{% endfor %}

## Resources
{% for res in resources %}
- `{{ res }}`
{% else %}
_None linked._
{% endfor %}

## Working with this project

Do NOT read repo `CLAUDE.md` files up front. When you are about to read, edit, or
run against files in a repo, FIRST read that repo's `CLAUDE.md`; load only the docs
for repos you actually touch.
{% if roadmap_path %}

## Roadmap

This project has a roadmap at `{{ roadmap_path }}`. Read it at the start of each
session to stay aligned with current goals. Mark items `[x]` as they are completed.
{% endif %}
